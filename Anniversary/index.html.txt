<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Happy Anniversary 💖</title>

<!-- Confetti CDN -->
<script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>

<style>
  * { box-sizing: border-box; }

  body {
    margin: 0;
    font-family: "Segoe UI", system-ui, sans-serif;
    background: #ffe9ee;
    transition: background 1.5s ease;
    overflow-x: hidden;
  }

  body.after-yes {
    background:
      linear-gradient(
        to right,
        rgba(255,233,238,0.55),
        rgba(255,233,238,0.15)
      ),
      url("wedding.jpg") center / cover no-repeat fixed;
  }

  .wrapper {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 20px;
  }

  .card {
    max-width: 480px;
    width: 100%;
    text-align: center;
    transition: all 1s ease;
  }

  body.after-yes .card {
    text-align: left;
    transform: translateX(-120px);
  }

  h1 {
    font-size: 2.3rem;
    margin-bottom: 12px;
  }

  .langs {
    font-size: 1.05rem;
    line-height: 1.6;
    margin-bottom: 18px;
  }

  .question {
    font-size: 1.1rem;
    margin-bottom: 24px;
  }

  #yes {
    padding: 22px 60px;
    font-size: 1.4rem;
    border-radius: 999px;
    border: none;
    background: #ff4f6d;
    color: white;
    cursor: pointer;
    box-shadow: 0 18px 40px rgba(255,79,109,0.45);
    transition: transform 0.6s ease, box-shadow 0.6s ease;
  }

  body.after-yes #yes {
    transform: scale(1.3);
    box-shadow: 0 28px 70px rgba(255,79,109,0.6);
  }

  .shy {
    margin-top: 16px;
    font-size: 0.95rem;
    opacity: 0.85;
  }

  .footer {
    margin-top: 26px;
    display: inline-block;
    padding: 10px 18px;
    font-size: 1rem;
    font-weight: 600;
    color: #b00032;
    background: rgba(255,255,255,0.85);
    border-radius: 14px;
  }

  #no {
    position: absolute;
    padding: 14px 28px;
    border-radius: 40px;
    background: #111;
    color: white;
    border: none;
    cursor: pointer;
    box-shadow: 0 10px 25px rgba(0,0,0,0.3);
  }

  @media (max-width: 600px) {
    body.after-yes .card {
      transform: translateX(-60px);
    }

    h1 { font-size: 1.9rem; }
  }
</style>
</head>

<body>

<div class="wrapper">
  <div class="card">
    <h1>💍💖💍<br>Happy Anniversary</h1>

    <div class="langs">
      🌸 திருமண நாள் நல்வாழ்த்துக்கள் ❤️<br>
      🌸 ವಿವಾಹ ವಾರ್ಷಿಕೋತ್ಸವದ ಶುಭಾಶಯಗಳು ❤️
    </div>

    <div class="question">
      Will you choose me again today? 😌
    </div>

    <button id="yes">Yes 💗</button>

    <div class="shy">
      “No” seems extra shy today 👀
    </div>

    <div class="footer">
      💞 From Chennai to Bangalore — one heart, one life 💞
    </div>
  </div>
</div>

<button id="no">😈 No 💜</button>

<audio id="song" src="jumbalaka.mp3" preload="auto"></audio>

<script>
  const noBtn = document.getElementById("no");
  const yesBtn = document.getElementById("yes");
  const song = document.getElementById("song");

  function moveNo() {
    noBtn.style.left = Math.random() * (window.innerWidth - 120) + "px";
    noBtn.style.top = Math.random() * (window.innerHeight - 80) + "px";
  }

  noBtn.addEventListener("mouseover", moveNo);
  noBtn.addEventListener("touchstart", moveNo);

  yesBtn.addEventListener("click", () => {
    // Background + layout
    document.body.classList.add("after-yes");

    // Confetti immediately
    confetti({
      particleCount: 250,
      spread: 120,
      origin: { y: 0.6 }
    });

    // Start song EXACTLY on click (no delay)
    song.currentTime = 30; // lyrics part
    song.play();

    // Popup AFTER effects
    setTimeout(() => {
      alert("Yeah that’s right 😌 You are married to me hahaha.");
    }, 600);
  });
</script>

</body>
</html>
